// chrome.extension.onMessage.addListener(function(message, sender, sendResponse) {
//   var contentWrapper = chrome.document.getElementsByClassName("content-wrapper");
//   console.log('---------> ' + contentWrapper.length);
//   contentWrapper.innerHTML = message.content;

//   // chrome.extension.ShowPageAction(message.content);
// });
